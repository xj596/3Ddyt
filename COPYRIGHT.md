# 3D导演台 · 版权与商用合规清单

> 目的：确认本仓库内**全部代码与资源均可商用**，不存在「禁止商用 / 非商业（CC-NC）/ 强 copyleft（GPL/AGPL）」等不可商用项。
> 扫描范围：本框架文件夹内的所有代码、第三方依赖引用、内置资源。

## 一、第三方依赖（代码内引用）

| 组件 | 版本 | 引入方式 | 许可证 | 是否可商用 |
|---|---|---|---|---|
| Three.js | 0.160.0 | CDN `cdn.jsdelivr.net/npm/three` | MIT | ✅ 可商用 |
| GLTFLoader / FBXLoader / OBJLoader（three/addons） | 0.160.0 | CDN（同包 examples/jsm） | MIT | ✅ 可商用 |

说明：
- Three.js 及示例加载器均为 MIT 许可，允许商用、修改、再分发，只需保留版权声明。
- 通过 jsdelivr CDN 加载 MIT 代码用于商业站点是被允许的。生产环境建议把 `three.module.js` 与 `examples/jsm/` 下载到 `public/lib/three/` 自托管，避免依赖外网（不影响许可合规性）。

## 二、内置资源

| 资源 | 授权 | 是否可商用 |
|---|---|---|
| 内嵌小人偶（studio.html 内联 base64） | 代码注释声明 CC0；同时属作者（用户）AI 自生成，权益归用户 | ✅ 可商用 |
| 4 个人偶模型 `public/models/man01~04.glb` | 与内嵌人偶同源（CC0 / 用户 AI 自生成） | ✅ 可商用 |
| 项目代码（前端脚本 / 配置 / 文档） | 本项目作者编写，自有版权 | ✅ 可商用 |

## 三、用户自备资源（⚠️ 需你把关）

- 道具模型库（家具 / 美食 / 山水自然 / 武器 等 `.glb`）：须为**你本人 AI 生成**，或取得**可商用授权**（CC0 / CC-BY / 商业授权）。
  - ✅ 允许：你用 AI 工具生成并保留生成记录、CC0、CC-BY、明确允许商用的付费素材。
  - ❌ **严禁**：CC-NC（非商业）、仅「个人学习/研究」授权、来源不明或未经授权的第三方模型。
- 将 `.glb` 放入 `public/models/` 并在 `library-manifest.json` 登记前，请确认其许可证可商用。

## 四、结论

当前仓库内**全部代码与依赖均允许商用**，可安全上线测试。
唯一风险点为后续自行添加的 `.glb` 模型授权，请按第三节核验后再上传。
